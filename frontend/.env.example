# Leave blank (or omit) in local development: the Vite proxy forwards all
# API requests to localhost:8000, eliminating CORS issues.
#
# Set this to the real backend URL only in production builds, e.g.:
#   VITE_API_URL=https://api.yourapp.com
VITE_API_URL=
